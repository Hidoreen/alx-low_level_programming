#include <stdio.h>
#include "main.h"

/**
 * puts2 - a function that prints every character of a string
 * @str: input
 * return: always 0
 */

void puts2(char *str)
{
	int = o;

	for (str[o] != '\0')
	{
		if ((o % 2) == 0)
		{
			_putchar (str[o]);
		else
			continue;
		}
		_putchar ('\n');
	}
}
