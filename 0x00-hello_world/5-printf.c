#include <stdio.h>
/**
* main - prints output using the comand printf
* return : return a value of 0
*/
int main (void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return(0);	
}
